// authReducer.js
const initialState = {
};

const annonceReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};


export default annonceReducer;